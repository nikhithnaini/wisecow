apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wisecow-ingress
spec:
  rules:
  - host: wise-cow.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: wisecow-service
            port:
              number: 80
